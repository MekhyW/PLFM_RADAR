(version 1)
(rule "class 1:0"
  (condition "A.NetClass == 'Differential' && B.NetClass == 'Default'")
  (constraint clearance (min 0.1mm)))

(rule "class 1:1"
  (condition "A.NetClass == 'Differential' && B.NetClass == 'Differential'")
  (constraint clearance (min 0.15mm)))
